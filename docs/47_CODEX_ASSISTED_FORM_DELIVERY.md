# Codex支援フォーム送信 実装記録

## 目的

通常HTMLフォームではない問い合わせ先も、Codexのコンピューター使用・ブラウザ操作を利用して、利用者が確認しながら送信できるようにする。

## 仕様

フォーム文面の画面には「Codex支援用の指示をコピー」を表示する。コピー内容には会社名、フォームURL、確認済み本文、送信前に確認すべき条件を含める。利用者はCodexでブラウザ操作を依頼し、その指示を貼り付ける。

Codexは専用の`leadhive-form-submit` Skillを使用し、フォームを開いて項目確認・入力・送信完了確認まで行う。利用者がコピー時の確認画面で一社分の最終送信を承認した場合だけ、送信ボタンを最大一回押す。CAPTCHAが表示された場合は利用者の操作を待つ。

送信後はLeadHiveで結果を「保留」「失敗」「送信済み」として記録する。送信済みだけを営業状況の「アプローチ済」に反映し、保留・失敗は後続の確認や再対応に残す。

## 対象

- CAPTCHAや複数画面を含むフォーム
- ログイン済みブラウザでしか開けないフォーム
- LeadHiveの通常フォーム送信が非対応と判定したフォーム

## 境界

Codex Skillはデスクトップの操作機能であり、LeadHiveバックエンドから直接起動・常時実行しない。したがって、定期実行や大量送信の仕組みには使わず、個別の確認付き送信に限定する。

## API

- `GET /api/outreach-drafts/{draft_id}/form-assist`
- `GET /api/outreach-drafts/{draft_id}/form-delivery`
- `POST /api/outreach-drafts/{draft_id}/form-assist-delivery`

フォームURL・本文・操作指示、または記録済みの送信結果を返す。結果記録には、Codex上で確認したことを示す承認が必要である。実際の送信はCodex上での確認操作に従う。
