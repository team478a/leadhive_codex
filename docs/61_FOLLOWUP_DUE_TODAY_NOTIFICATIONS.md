# 本日追客通知 実装記録

## 目的

設定済みの次回対応日時を期限超過前に確認できるようにし、返信後の追客を当日中に進めやすくする。

## 動作

ダッシュボードの通知一覧を開くと、日本時間で当日中かつ現在時刻以後に期限を迎える追客タスクを「本日のフォロー予定」として表示する。通知にはプロジェクト、時刻、担当者を表示する。

期限を過ぎたタスクは従来どおり「フォロー期限超過」として別に通知する。同じ期限と利用者の組み合わせでは一度だけ作成される。通知はプロジェクトの所有者・メンバーごとに作成される。
